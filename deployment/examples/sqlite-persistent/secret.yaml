apiVersion: v1
kind: Secret
metadata:
  name: database-config
  labels:
    app.kubernetes.io/name: maas-api
    app.kubernetes.io/component: config
type: Opaque
stringData:
  # SQLite database file path (must match the volume mount path)
  DATABASE_URL: "sqlite:///data/maas-api.db"

